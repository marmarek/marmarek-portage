diff -arc Mail-SpamAssassin-3.1.2/lib/Mail/SpamAssassin/Conf/Parser.pm Mail-SpamAssassin-3.1.2.new/lib/Mail/SpamAssassin/Conf/Parser.pm
*** Mail-SpamAssassin-3.1.2/lib/Mail/SpamAssassin/Conf/Parser.pm	Wed May 24 22:07:28 2006
--- Mail-SpamAssassin-3.1.2.new/lib/Mail/SpamAssassin/Conf/Parser.pm	Mon May 29 09:56:41 2006
***************
*** 209,217 ****
    # See http://www.gnu.org/manual/glibc-2.2.5/html_node/Locale-Categories.html
    # and http://www.gnu.org/manual/glibc-2.2.5/html_node/Using-gettextized-software.html
    my $lang = $ENV{'LANGUAGE'}; # LANGUAGE has the highest precedence but has a
!   if ($lang) {                 # special format: The user may specify more than
!     $lang =~ s/:.*$//;         # one language here, colon separated. We use the
!   }                            # first one only (lazy bums we are :o)
    $lang ||= $ENV{'LC_ALL'};
    $lang ||= $ENV{'LC_MESSAGES'};
    $lang ||= $ENV{'LANG'};
--- 211,221 ----
    # See http://www.gnu.org/manual/glibc-2.2.5/html_node/Locale-Categories.html
    # and http://www.gnu.org/manual/glibc-2.2.5/html_node/Using-gettextized-software.html
    my $lang = $ENV{'LANGUAGE'}; # LANGUAGE has the highest precedence but has a
!   if (0) { # We support multiple languages :)
!       if ($lang) {                 # special format: The user may specify more than
! 	  $lang =~ s/:.*$//;         # one language here, colon separated. We use the
!       }                            # first one only (lazy bums we are :o)
!   }
    $lang ||= $ENV{'LC_ALL'};
    $lang ||= $ENV{'LC_MESSAGES'};
    $lang ||= $ENV{'LANG'};
***************
*** 222,227 ****
--- 226,236 ----
    } else {
      $lang =~ s/[@.+,].*$//;    # Strip codeset, modifier/audience, etc.
    }                            # (eg. .utf8 or @euro)
+   if ($conf->{mail_languages}) {
+       $lang=$conf->{mail_languages};
+   }
+ 
+   my $desc_lang=$conf->{desc_language};
  
    # get fast-access handles on the command lookup tables
    my $lut_frequent = $self->{command_luts}->{frequent};
***************
*** 245,251 ****
      next unless($line); # skip empty lines
  
      # handle i18n
!     if ($line =~ s/^lang\s+(\S+)\s+//) { next if ($lang !~ /^$1/i); }
  
      my($key, $value) = split(/\s+/, $line, 2);
      $key = lc $key;
--- 254,269 ----
      next unless($line); # skip empty lines
  
      # handle i18n
!     if ($line =~ s/^lang\s+(\S+)\s+//) { 
! 	my $tmp_lang=$1;
! 	if ($line =~ /^describe/i) {
! 	    next if ($desc_lang !~ /$tmp_lang/i);
! 	} else {
! 	    if ($lang !~ /all/i) {
! 		next if ($lang !~ /$tmp_lang/i);
! 	    }
! 	}
!     }
  
      my($key, $value) = split(/\s+/, $line, 2);
      $key = lc $key;
diff -arc Mail-SpamAssassin-3.1.2/lib/Mail/SpamAssassin/Conf.pm Mail-SpamAssassin-3.1.2.new/lib/Mail/SpamAssassin/Conf.pm
*** Mail-SpamAssassin-3.1.2/lib/Mail/SpamAssassin/Conf.pm	Wed May 24 22:07:29 2006
--- Mail-SpamAssassin-3.1.2.new/lib/Mail/SpamAssassin/Conf.pm	Mon May 29 09:49:02 2006
***************
*** 2610,2615 ****
--- 2610,2644 ----
      }
    });
  
+ =item mail_languages
+ 
+ Use body text filters from this languages.
+ 
+ You can specify multiple values separated with colon.
+ 
+ Example: mail_languages en:pl:es
+ 
+ =cut
+ 
+   push (@cmds, {
+     setting => 'mail_languages',
+     default => 'all',
+     type => $CONF_TYPE_STRING
+   });
+   
+ =item desc_language
+ 
+ Use descriptions in this language
+ 
+ =cut
+ 
+   push (@cmds, {
+     setting => 'desc_language',
+     default => 'none',
+     type => $CONF_TYPE_STRING
+   });
+ 
+ 
  =back
  
  =head1 TEMPLATE TAGS
